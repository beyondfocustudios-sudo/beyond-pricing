# ============================================================
# Beyond Pricing — Environment Variables
# ============================================================
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to git.
# ============================================================

# ── Supabase ─────────────────────────────────────────────────
# Format: https://<project-ref>.supabase.co
# Found at: Supabase Dashboard → Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co

# Found at: Supabase Dashboard → Settings → API → anon / public key
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Found at: Supabase Dashboard → Settings → API → service_role key
# ⚠️  SERVER-ONLY — never expose in client code or commit to git
# Used by /api/admin/create-user to provision client portal users
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ── Dropbox (required for Entregas/Deliverables feature) ─────
# Found at: Dropbox Developer Console → your app → Settings
DROPBOX_APP_KEY=your_dropbox_app_key
DROPBOX_APP_SECRET=your_dropbox_app_secret
# Offline refresh token — generate via OAuth2 flow (see README)
DROPBOX_REFRESH_TOKEN=your_offline_refresh_token
# Root folder path in Dropbox where client files are stored
DROPBOX_BASE_PATH=/Beyond/Clients

# ── OpenAI (optional — AI photo tagging beta) ────────────────
# Found at: https://platform.openai.com/api-keys
# Leave empty to disable AI tagging feature entirely
OPENAI_API_KEY=sk-proj-...

# ── External Calendar Sync (Google + Microsoft + ICS) ─────────
# Secret for encrypting provider refresh/access tokens at rest (server-only)
CALENDAR_TOKEN_SECRET=change_me_to_a_long_random_secret

# Google Calendar OAuth app credentials
GOOGLE_CALENDAR_CLIENT_ID=your_google_calendar_client_id
GOOGLE_CALENDAR_CLIENT_SECRET=your_google_calendar_client_secret

# Microsoft Calendar OAuth app credentials
MICROSOFT_CALENDAR_CLIENT_ID=your_microsoft_calendar_client_id
MICROSOFT_CALENDAR_CLIENT_SECRET=your_microsoft_calendar_client_secret
# Optional tenant id, default is \"common\"
MICROSOFT_CALENDAR_TENANT_ID=common
